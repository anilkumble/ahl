

<%= form_for([@match,@photo]) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!--<div class="form-group">
    <%#= f.label :match_id, class: "control-label"%><br>
    <%#= f.number_field :match_id, class: "form-control"%>
</div>-->
  <div class="form-group">
    <%= f.label :photo_url, class: "control-label" %><br>
    <%= f.text_field :photo_url, autofocus: true, class: "form-control"%>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-danger" %>
  </div>
<% end %>
